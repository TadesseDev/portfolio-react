import styled from "styled-components";
const about = styled.div`
  text-align: center;
  p {
    color: white;
  }
  .bright {
    color: var(--bright);
  }
  .emoji {
    font-size: 2rem;
  }
`;
export default about;
